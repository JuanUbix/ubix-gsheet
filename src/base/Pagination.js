class Pagination {
    /**
     * @constructor
     * @param {number} limit
     * @param {number} offset
     */
    constructor(limit, offset) {
        this.limit = limit
        this.offset = offset
    }
}